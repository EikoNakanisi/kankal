<h1>Listing details</h1>

<table border="1">
  <tr>
    <th>planID</th>
    <th>detailID</th>
    <th>看護問題名</th>
    <th></th>
    <th></th>

  </tr>

<% @details.each do |detail| %>
  <tr>
    <td><%= detail.plan %></td>
    <td><%= detail.id %></td>
    <td><%= detail.kanren3 %></td>
    <td><%= link_to 'Show', plan_detail_path(detail.plan, detail) %></td>
    <td><%= link_to 'Edit', edit_plan_detail_path(detail.plan, detail) %></td>
    <!--    <td><%= link_to 'Destroy', plan_detail_path(detail.plan, detail), method: :delete, data: { confirm: 'Are you sure?' } %></td>  -->
  </tr>
<% end %>
</table>

<br />

<%#= link_to 'New detail', new_detail_path %>
<%= link_to 'New detail', new_plan_detail_path %>|
<%= link_to 'PlanIndex', plans_path %>|

