<h1>Listing plans</h1>

<table border="1">
  <tr>
    <th>Name</th>
    <th>npn01</th>
    <th>npn02</th>
    <th>npn03</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @plans.each do |plan| %>
  <tr>
    <td><%= plan.id %></td>
    <td><%= plan.npn01 %></td>
    <td><%= plan.npn02 %></td>
    <td><%= plan.npn03 %></td>
    <!-- added next line -->
    <td><%= link_to 'Show books', plan_books_path(plan) %></td>
    
    <td><%= link_to 'Show', plan %></td>
    <td><%= link_to 'Edit', edit_plan_path(plan) %></td>
<!--     <td><%= link_to 'Destroy', plan, method: :delete, data: { confirm: 'Are you sure?' } %></td>
 -->
  </tr>
<% end %>
</table>

<br/>

<%= link_to 'New plan', new_plan_path %>|

<%= link_to 'patienthome', patients_path %>